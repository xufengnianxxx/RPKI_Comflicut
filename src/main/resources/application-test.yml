spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/rpki_db_test?useSSL=false&serverTimezone=UTC
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver

rpki:
  sync:
    cron: "0 0 3 * * ?"

fabric:
  mode: MOCK
