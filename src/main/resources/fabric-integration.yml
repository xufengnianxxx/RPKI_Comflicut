spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/rpki_db?useSSL=false&serverTimezone=UTC&characterEncoding=utf-8&allowMultiQueries=true
    username: root
    password: rpki123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

# Fabric 配置
fabric:
  enabled: true  # 真实 Fabric 集成已激活
  gateway:
    connection-profile: fabric-config.json
  user:
    name: admin
    msp-id: Org1MSP
  chaincode:
    name: rpkiccjava
    channel: mychannel
  network:
    host: 127.0.0.1
    peer-port: 7051
    orderer-port: 7050
    ca-port: 7054

logging:
  level:
    com.rpki: DEBUG
