{
  "name": "rpki-caliper",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "npx caliper launch manager --caliper-workspace . --caliper-networkconfig network.json --caliper-benchconfig benchmark-config.yaml --caliper-flow-only-test"
  },
  "dependencies": {
    "@hyperledger/caliper-cli": "^0.6.0",
    "@hyperledger/caliper-core": "^0.6.0"
  }
}
